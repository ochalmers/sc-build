# Shared workspace comments (GitHub Gist backend)
# Create a classic PAT with the `gist` scope, or use: gh auth token
COMMENTS_GITHUB_TOKEN=
COMMENTS_GIST_ID=5253e9bc9987baf2d4a4ff0007aa5098

# Optional client-side write fallback (avoid on public deploys if possible)
# VITE_COMMENTS_GITHUB_TOKEN=
